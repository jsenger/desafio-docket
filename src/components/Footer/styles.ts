import styled from 'styled-components';

export const FooterContainer = styled.footer`
  width: 100%;
  padding: 8px 0;
  background: transparent linear-gradient(90deg, var(--purple-60) 0%, var(--purple-80) 100%) 0% 0% no-repeat padding-box;

  text-align: center;
  color: var(--white);
  font-weight: 700;
  font-size: 1.3rem;
`;
